dbs:
  - path: /pb/pb_data/data.db
    replicas:
      - url: "s3://${S3_BUCKET_NAME}/db"
        endpoint: "${S3_ENDPOINT}"
        access-key-id: "${AWS_ACCESS_KEY_ID}"
        secret-access-key: "${AWS_SECRET_ACCESS_KEY}"
        force-path-style: true